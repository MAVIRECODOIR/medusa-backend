diff --git a/node_modules/@easypayment/medusa-payment-paypal/.medusa/server/src/modules/paypal/utils/currencies.js b/node_modules/@easypayment/medusa-payment-paypal/.medusa/server/src/modules/paypal/utils/currencies.js
index 671d6ab..a95f3f5 100644
--- a/node_modules/@easypayment/medusa-payment-paypal/.medusa/server/src/modules/paypal/utils/currencies.js
+++ b/node_modules/@easypayment/medusa-payment-paypal/.medusa/server/src/modules/paypal/utils/currencies.js
@@ -46,9 +46,6 @@ function getPayPalCurrencyCompatibility(input) {
     if (!isPayPalCurrencySupported(currency)) {
         errors.push(`PayPal does not support currency "${currency}".`);
     }
-    if (overrideCurrency && overrideCurrency !== currency) {
-        errors.push(`PayPal is configured for "${overrideCurrency}", but the store cart uses "${currency}".`);
-    }
     return {
         currency,
         overrideCurrency,
